name: Debug Task 9 Ruff

# Temporary Task 9 diagnostic; remove after the exact lint finding is captured.
on:
  pull_request:

permissions:
  contents: read

jobs:
  debug-task9-ruff:
    if: github.event.pull_request.number == 13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          version: "0.10.0"
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.12"
      - run: uv sync --all-groups --frozen
      - name: Capture Task 9 Ruff output
        shell: bash
        run: |
          set +e
          uv run ruff check src/gemini_trading/research/engine.py tests/unit/research/test_engine.py tests/integration/test_backtest_event_order.py tests/regression/test_duplicate_cycle_idempotency.py --output-format=full > task9-ruff-output.txt 2>&1
          status=$?
          echo "exit_status=$status" >> task9-ruff-output.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: task9-ruff-output
          path: task9-ruff-output.txt
          if-no-files-found: error
          retention-days: 1
