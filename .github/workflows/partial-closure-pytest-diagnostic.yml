name: Partial Closure Pytest Diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  pytest:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.12"
      - run: uv sync --all-groups --frozen
      - name: Capture pytest report
        shell: bash
        run: |
          set +e
          uv run pytest -q --tb=short > /tmp/partial-closure-pytest.txt 2>&1
          status=$?
          cat /tmp/partial-closure-pytest.txt
          echo "$status" > /tmp/partial-closure-pytest-status.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: partial-closure-pytest-${{ github.sha }}
          path: |
            /tmp/partial-closure-pytest.txt
            /tmp/partial-closure-pytest-status.txt
          if-no-files-found: error
          retention-days: 1
