name: Temporary lint diagnostic sealed validation

on:
  push:
    branches: [research/sealed-btcusdt-historical-validation]

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: research/sealed-btcusdt-historical-validation
          fetch-depth: 0
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          version: "0.11.25"
          enable-cache: true
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.12"
      - run: uv sync --all-groups --frozen
      - name: Capture remaining lint findings
        run: |
          uv run ruff check . > reports/verification/sealed-validation-lint-diagnostic.txt 2>&1 || true
          git rm .github/workflows/temporary-lint-diagnostic-sealed-validation.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: capture sealed validation lint diagnostics"
          git push origin HEAD:research/sealed-btcusdt-historical-validation
